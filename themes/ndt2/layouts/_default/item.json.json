{
    "name": "{{ .Title | htmlEscape }}",
    "permalink" : "{{ .Slug }}",
    "lat": "{{ .Params.lat }}",
    "lng": "{{ .Params.lng }}"{{ if .Params.poster }},
    "poster": "{{ .Params.poster }}"
    {{ end }}
}